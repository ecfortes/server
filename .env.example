# Base variables applied before environment-specific files (.env.development, .env.production)
# Use this file for values shared across environments or as um guia para montar .env.*

# Se usar URL completa, nao precisa preencher PGHOST/PGPORT/PGUSER/PASSWORD/DATABASE
# Exemplo: postgres://usuario:senha@192.168.1.20:5432/pallet_db
DATABASE_URL=

# Caso prefira variaveis separadas (com IP em rede local)
PGHOST=192.168.1.20
PGPORT=5432
PGUSER=usuario
PGPASSWORD=senha
PGDATABASE=pallet_db

# Porta exposta pela API Express
PORT=4000

# Origem do frontend liberada no CORS (use IP + porta do Vite/build)
# Exemplo dev: http://192.168.1.50:5173
# Exemplo build: http://192.168.1.50:4173
CLIENT_ORIGIN=http://127.0.0.1:5173

# Flags opcionais de log SQL
SQL_LOG=false
SQL_SLOW_MS=1000
SQL_PARAMS=false
